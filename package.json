{
  "name": "web-chat-service",
  "version": "1.0.0",
  "description": "Web Chat Service",
  "main": "server.js",
  "standard": {
    "ignore": [
      "/public/*.js"
    ]
  },
  "scripts": {
    "test": "standard && nyc --report=html --reporter=text mocha tests",
    "test:dev": "mocha tests --recursive --watch",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "build": "uglifyjs *.js -c -m -o dist/web-chat-service.min.js --source-map \"filename='dist/web-chat-service.min.js.map'\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/leocwlam/web-chat-service.git"
  },
  "author": "Leo Lam",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "moment": "^2.28.0",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.1.0",
    "mocha": "^7.1.2",
    "nyc": "^15.1.0",
    "sinon": "^9.0.3",
    "standard": "^14.3.4",
    "uglify-es": "^3.3.9"
  }
}
